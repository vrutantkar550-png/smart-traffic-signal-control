# Frontend environment variables
# Copy to .env and fill in values

# Backend API base URL (used during local dev outside Docker)
VITE_API_BASE=http://localhost:8000

# WebSocket base URL
VITE_WS_BASE=ws://localhost:8000
